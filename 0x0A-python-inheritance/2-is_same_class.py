#!/usr/bin/python3
""" function that returns true otherwise false """


def is_same_class(obj, a_class):
    """ Represent a function """
    if type(obj) == a_class:
        return True
    return False
